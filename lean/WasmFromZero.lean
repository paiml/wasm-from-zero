import WasmFromZero.Theorems.Canvas
import WasmFromZero.Theorems.Lifecycle
import WasmFromZero.Theorems.Panels

/-!
# WasmFromZero — Lean 4 root

Pulls in the three per-pillar theorem modules. Each theorem mirrors a
`proof_obligations[*].lean.theorem` entry in the corresponding YAML
contract (`contracts/wasm-{rendering,lifecycle,panels}-v1.yaml`).
-/
